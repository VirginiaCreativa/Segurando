import React from 'react';
import classes from './postscontent.module.scss';

const PostsContentItem = ({ title }) => (
  <div className={classes.PostsContenItem} />
);

export default PostsContentItem;
